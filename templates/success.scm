(layout
 title

 `(section

   (h2 ,message)

   (div
    (@ (class "actions"))

    (a
     (@ (class "button")
        (href "/"))
     "Dashboard")

    (a
     (@ (class "button")
        (href "/login"))
     "Login"))))
